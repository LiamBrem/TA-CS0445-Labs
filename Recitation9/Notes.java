public class Notes{
	public static void main(String[] args) {
		// Declaring an array: (get size from user input)
		Integer[] arr = new Integer[size];

		// Fill array w/ random values
		for (int i = 0; i < size; i++) {
		    int value = rand.nextInt(1000); // 1-1000 (can put any # here)
		}

		// instantiate TexMerge object
		TextMerge sorter = new TextMerge();

		long start = System.nanoTime(); // start the timer
		// SORT	
		long stop = System.nanoTime(); // end timer
		long delta = stop - start; // total time elapsed (nanoseconds)
	}
}
